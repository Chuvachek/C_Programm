#include <stdio.h> // Это "библиотека", чтобы компьютер знал, что такое printf

int main() {
    // Весь ваш код должен быть внутри этих фигурных скобок
    int n = 5;
    n = n + 1;
    
    printf("Result is %d\n", n); 

    return 0; // Признак того, что программа завершилась успешно
}